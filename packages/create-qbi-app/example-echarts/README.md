# Quick BI Custom Chart

## Setup

Install the dependencies:

```bash
pnpm install
```

## Get started

Start dev server:

```bash
pnpm run start
```

Build the component:

```bash
pnpm run build
```

Bundle the component:

```bash
pnpm run bundle
```
